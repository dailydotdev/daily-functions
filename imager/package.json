{
  "name": "daily-imager",
  "main": "index.js",
  "private": true,
  "scripts": {
    "deploy": "gcloud --project devkit-prod beta functions deploy imager --trigger-resource crawled-post --trigger-event google.pubsub.topic.publish --set-env-vars BUCKET=devkit-assets"
  },
  "engines": {
    "node": "~6.14.0"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^0.19.0",
    "@google-cloud/storage": "^1.7.0",
    "request": "^2.87.0",
    "request-promise-native": "^1.0.5",
    "sharp": "^0.20.5"
  }
}
