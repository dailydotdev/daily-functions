{
  "name": "daily-webhook",
  "main": "index.js",
  "private": true,
  "scripts": {
    "deploy": "gcloud --project devkit-prod beta functions deploy webhook --trigger-http --env-vars-file .env.yaml"
  },
  "engines": {
    "node": "~6.14.0"
  },
  "devDependencies": {
    "request": "^2.87.0"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^0.19.0"
  }
}
